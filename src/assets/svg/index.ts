const assets = {
  EXCHANGE: require('./exchange').default,
  SECURITY: require('./security').default,
  SIMPLICITY: require('./simplicity').default,
  TRANSPARENCY: require('./transparency').default,
};

export default assets;
